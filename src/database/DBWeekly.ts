class DBWeekly {
    // create weekly clear data collection
    // use maintenance to keep track of current week
    // on init of this class and maybe also occasionly check which week current week is???

    getFullClearData() {
        return {};
    }
}

const dbWeekly = new DBWeekly();

export default dbWeekly;
